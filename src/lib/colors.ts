import { getColor, getRoot, getOptimalTextColor } from "./assets/scripts/colors"
import type { Colors, Depths } from "./assets/scripts/colors"

export {
    getColor,
    getRoot,
    getOptimalTextColor
}

export type {
    Colors, Depths
}